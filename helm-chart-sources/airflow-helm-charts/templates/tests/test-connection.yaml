apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airflow-helm-charts.fullname" . }}-test-connection"
  labels:
{{ include "airflow-helm-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "airflow-helm-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
