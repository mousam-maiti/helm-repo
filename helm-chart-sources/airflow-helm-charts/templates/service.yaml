apiVersion: v1
kind: Service
metadata:
  name: {{ include "airflow-helm-charts.fullname" . }}
  labels:
    {{- include "airflow-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airflow-helm-charts.selectorLabels" . | nindent 4 }}
